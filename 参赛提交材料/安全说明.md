# 安全说明

## 安全检查结果

✅ **通过检查** — 参赛提交材料中不包含以下敏感内容：

| 检查项 | 状态 | 说明 |
|--------|------|------|
| `.env` 文件 | ✅ 已排除 | 仅保留 `.env.example`（模板，无真实密钥） |
| `node_modules/` | ✅ 已排除 | 所有 node_modules 已删除 |
| `*.db` / `*.db-shm` / `*.db-wal` | ✅ 已排除 | SQLite 数据库文件已删除，仅保留 schema |
| `.DS_Store` | ✅ 已排除 | macOS 系统文件已清除 |
| Coze PAT Token | ✅ 已排除 | 真实 PAT 仅在本地 `.env` 中，未提交 |
| OpenClaw Token | ✅ 未发现 | 提交材料中无 OpenClaw 凭据 |
| 飞书 App Secret | ✅ 未发现 | 提交材料中无飞书凭据 |
| 其他真实密钥 | ✅ 未发现 | 所有接口均为演示模式，无真实 API Key |

## 安全规范遵循

- ✅ 只提交 `.env.example`，不提交 `.env`
- ✅ 所有 API 请求使用模拟数据，无需真实服务端
- ✅ 演示模式默认开启（`DEMO_MODE=true`），无需连接真实数据库
- ✅ 演示账号密码为公开测试密码，不涉及真实用户数据

## 注意事项（评委/使用方）

1. **部署到公网前**：请务必：
   - 更换所有演示账号密码
   - 填写真实的 Coze 配置（如有需要）
   - 配置 HTTPS 证书

2. **本地环境变量配置**：如使用完整后端，需执行：
   ```bash
   cp .env.example .env
   ```
   然后在 `.env` 中填入实际配置。

3. **提交材料仅用于比赛评审**，请勿在生产环境中直接使用。

## 排除文件清单

以下文件已从参赛提交包中排除：

```
node_modules/
.env
*.db
*.db-shm
*.db-wal
.DS_Store
npm-debug.log
```
